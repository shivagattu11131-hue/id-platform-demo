FROM python:3.11-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*
COPY legacy-ma-site/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY legacy-ma-site/ .
EXPOSE 3002
CMD ["python", "app.py"]
